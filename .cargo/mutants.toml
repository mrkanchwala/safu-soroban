# cargo-mutants configuration — documented equivalent-mutant exclusions.
#
# Added 2026-07-15 after the first full mutation run (464 mutants, 60
# missed). 51 of the 60 are killed by targeted tests in
# src/test/mutation_gap_tests.rs. The 9 excluded below are PROVABLY
# EQUIVALENT: the mutated code has no observable behavioral difference
# from the original under any reachable contract state. Each exclusion
# carries its justification — do not add entries here without one.
#
# NOTE: patterns match cargo-mutants' mutant descriptions. They are
# line-number-free on purpose (source-line drift safe) but therefore
# scoped by function — if a function later gains a NEW mutation site
# matching one of these patterns that is NOT equivalent, it would be
# wrongly excluded. Re-review this file whenever the named functions
# change.

exclude_re = [
    # lib.rs get_points_balance / is_eligible / is_claim_eligible:
    # `r.amount > 0` → `>= 0`. Equivalent because amount == 0 with
    # withdrawn == false is unreachable: stake() requires amount > 0;
    # only withdraw()/emergency_exit() zero the amount and both set
    # withdrawn = true in the same write; claim forfeiture sets
    # withdrawn = true WITHOUT zeroing amount (activate_claim doc).
    # With amount == 0 implying withdrawn == true, the `!withdrawn`
    # conjunct already rejects every state the >= would newly admit.
    "replace > with >= in ProtectionPool::get_points_balance",
    "replace > with >= in ProtectionPool::is_eligible",
    "replace > with >= in ProtectionPool::is_claim_eligible",

    # stake.rs withdraw: `if final_points > 0 { set_points_balance(banked +
    # points) }` → `>=`. Equivalent: when points == 0 the mutant writes
    # banked + 0 — the same value the original leaves in place
    # (get_points_balance falls back to unwrap_or(0) for an absent entry, so
    # entry-existence itself is not observable either).
    #
    # REMOVED 2026-08-24: the companion "replace > with >= in activate_claim"
    # entry. Its target (`if points > 0` inside activate_claim) no longer
    # exists — claim.rs:394 now burns the balance unconditionally
    # (`set_points_balance(env, &claim.wallet, 0)`, the 2026-07-22 Rule A
    # change). activate_claim's body contains no `>` operator at all, so the
    # pattern matched zero mutants; leaving it would silently swallow a real
    # mutant if that function ever regains a `>` comparison.
    "replace > with >= in withdraw",

    # stake.rs compute_points_for_record entry guard: `record.amount <= 0
    # || record.withdrawn` → `&&`. Equivalent because the guard is
    # defensive-only — every reachable call site already excludes both
    # conditions before calling: activate_claim is only invoked on
    # non-withdrawn records with amount > 0 (submit_claim/unlock/override
    # all check first), withdraw() panics on amount <= 0 before computing
    # points, and lib.rs::get_points_balance guards amount > 0 &&
    # !withdrawn itself. No public path reaches this function with either
    # condition true, so weakening || to && is unobservable.
    "replace \\|\\| with && in compute_points_for_record",

    # stake.rs compute_points_for_record day-tier branches:
    # `if d > 90/180/365 { d.min(...) - N } else { 0 }` → `>=`.
    # Equivalent: at d == N the then-branch computes d.min(M) − N = 0,
    # identical to the else-branch. Zero-width boundary. The `==` and
    # arithmetic mutants at the same lines are NOT excluded — they are
    # killed by points_formula_exact_values_at_all_day_tier_boundaries.
    "replace > with >= in compute_points_for_record",

    # stake.rs stake() re-stake guard (line 137 ONLY — re-anchored
    # 2026-07-31 after the typed-errors-conversion refactor shifted
    # this line from 126; the pool-cap check at line 145 also mutates
    # `>`→`>=` but is catchable and IS tested; the line anchor keeps
    # it in scope): `existing.amount > 0 && !existing.withdrawn` →
    # `>= 0`. Equivalent by the same amount==0 ⇒ withdrawn==true
    # invariant as the lib.rs views above. LINE-NUMBER SENSITIVE:
    # re-anchor if stake.rs lines shift.
    "stake\\.rs:137:.*replace > with >= in stake",

    # 11th exclusion, added 2026-07-22 after the points-burn-mechanism
    # mutation re-run (485 mutants, 10 missed — 9 real gaps closed with
    # new tests, this is the 10th). lib.rs get_points_balance: `record
    # .amount > 0 && !record.withdrawn` → `||`. Verified equivalent by
    # full truth-table walk, NOT the same reasoning as the entry above
    # (that one relies on amount==0 being unreachable with withdrawn==
    # false; this mutant's only reachable divergent state is the OTHER
    # direction — amount > 0 with withdrawn == true, now reachable since
    # 2026-07-22's claim-forfeiture path sets withdrawn=true without
    # zeroing amount). In that state the `&&`/`||` mutation genuinely
    # takes different branches (skip vs. live-compute) — but
    # compute_points_for_record's OWN entry guard (excluded above)
    # independently returns 0 whenever withdrawn==true, and activate_claim
    # unconditionally zeroes the banked balance in the same transaction
    # that sets withdrawn=true. So the correct branch (banked, always 0
    # here) and the mutant's branch (live-compute, also forced to 0 by
    # the inner guard) produce the IDENTICAL return value in every
    # reachable state — no test can distinguish them.
    "replace && with \\|\\| in ProtectionPool::get_points_balance",

    # ---------------------------------------------------------------
    # Added 2026-08-24 after the full 144-mutant T3 campaign (134 caught,
    # 9 missed, 1 unviable). Six of the nine misses were REAL gaps and were
    # closed with tests in src/test/t3_flags_tests.rs. These three are the
    # provably-equivalent remainder.
    # ---------------------------------------------------------------

    # vault.rs auto_deploy_liquidity + ensure_liquidity bootstrap guard:
    # `deployed_shares <= 0 || deployed_xlm <= 0` → `&&`. Equivalent
    # because the two figures are always both-zero or both-nonzero, so no
    # state exists where one guard admits and the other rejects:
    #   • every deposit writes both together (vault.rs:500/501 and
    #     595/596), and a deposit that mints zero shares is rejected by the
    #     min_shares_out floor before those writes are reached;
    #   • every redeem writes both together (655/656) using
    #     principal_equiv = deployed_xlm * shares / deployed_shares (638),
    #     which on a FULL redeem (shares == deployed_shares) equals
    #     deployed_xlm exactly — both land on zero in the same step;
    #   • a PARTIAL redeem leaves both strictly positive, since
    #     principal_equiv truncates DOWN and so stays below deployed_xlm.
    "replace \\|\\| with && in auto_deploy_liquidity",
    "replace \\|\\| with && in ensure_liquidity",

    # vault.rs auto_deploy_liquidity allocation guard, the SUBTRACTION only
    # (`liquid - amount` → `liquid + amount` at vault.rs:563:15). The
    # original condition is unreachable-false, and so is the mutant:
    #   idle = (liquid - total_allocated).max(0), and the function already
    #   returned at `idle == 0`, so idle == liquid - total_allocated > 0.
    #   amount = idle.min(room) <= idle, therefore
    #   liquid - amount >= liquid - idle == total_allocated — never `<`.
    #   The mutant `liquid + amount < total_allocated` is likewise always
    #   false, since liquid >= total_allocated and amount > 0.
    #
    # SCOPED TO THE SUBTRACTION DELIBERATELY. The COMPARISON mutants on this
    # same line (563:24, `<` → `==` and `<` → `<=`) are NOT equivalent:
    # both flip TRUE when amount == idle, which is reachable whenever ceiling
    # room exceeds idle. They are real gaps, now killed by
    # auto_deploy_liquidity_deploys_exactly_idle_when_idle_is_the_binding_constraint.
    # A line-wide exclusion here would have hidden two genuine defects —
    # triage per-mutant, never per-line.
    #
    # LINE-NUMBER SENSITIVE (same caveat as the stake.rs:137 entry above):
    # the 563:15 anchor is what keeps this scoped to the subtraction and
    # off the two catchable comparison mutants at 563:24. Re-anchor if
    # vault.rs lines shift.
    "vault\\.rs:563:15:.*replace - with \\+ in auto_deploy_liquidity",
]
